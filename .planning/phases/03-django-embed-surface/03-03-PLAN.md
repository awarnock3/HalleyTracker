---
plan_id: 03-03
title: Django templatetag wiring path
wave: 1
autonomous: true
files_modified:
  - src/halley_tracker/templatetags/__init__.py
  - src/halley_tracker/templatetags/halley_tracker.py
  - tests/test_django_templatetags.py
---

## Objective

Provide direct Django template wiring for host apps via a `templatetags` entrypoint.

## Tasks

- [x] Add `templatetags/halley_tracker.py` with `halley_tracker_block` template tag.
- [x] Support environments without Django by graceful fallback.
- [x] Add tests for tag callable behavior.

## Success Checks

- [x] Host Django app can load `{% load halley_tracker %}` and render block tag.
- [x] Tag path works in non-Django environments for local testing/dev.
- [x] Tests pass for tag module behavior.
